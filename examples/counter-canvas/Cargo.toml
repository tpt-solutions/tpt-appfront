[package]
name = "counter-canvas"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
appfront-core = { path = "../../crates/appfront-core" }
appfront-canvas = { path = "../../crates/appfront-canvas" }
