[package]
name = "appfront-html"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Semantic HTML/SSR backend for TPT AppFront"
readme = "README.md"

[dependencies]
appfront-core = { path = "../appfront-core", version = "0.1.0" }
